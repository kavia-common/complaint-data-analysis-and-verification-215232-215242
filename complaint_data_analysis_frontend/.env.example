# Frontend environment configuration
# Primary API base for backend integration. Should point to your Flask backend base URL.
# For local dev with backend on port 3001:
REACT_APP_API_BASE=http://localhost:3001

# Legacy fallback (deprecated) - only used if REACT_APP_API_BASE is not set.
# REACT_APP_BACKEND_URL=http://localhost:3001
